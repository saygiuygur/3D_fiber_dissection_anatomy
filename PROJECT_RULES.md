Aim: interactive anatomy viewer with sequential loading, annotations, navigator, etc.
Locked features (do not change unless asked):
camera carry-over + initial auto-frame behavior
loading circle / pause / green tick icons
download click behaviors
annotation behavior (positions, occlusion policy, selected always visible, etc.)
annotation navigator UI behavior
focus ellipse behavior (AP/SI sizes, medial shifts per model)
